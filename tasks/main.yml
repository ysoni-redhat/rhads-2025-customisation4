---
- name: Download ArgoCD CLI binary
  get_url:
    url: "https://github.com/argoproj/argo-cd/releases/download/{{ argocd_version }}/argocd-linux-amd64"
    dest: /usr/local/bin/argocd
    mode: '0755'

- name: Gather Keycloak PVCs
  command: >
    oc get pvc -n {{ namespace }} -l postgres-operator.crunchydata.com/cluster=keycloak -o json
  register: keycloak_pvcs_raw

- name: Set fact with parsed PVCs
  set_fact:
    keycloak_pvcs: "{{ (keycloak_pvcs_raw.stdout | from_json)['items'] }}"

- name: Patch all Keycloak PVCs to increase storage
  command: >
    oc patch pvc {{ item.metadata.name }}
    -n {{ namespace }}
    --type=merge
    -p '{"spec": {"resources": {"requests": {"storage": "{{ new_size }}"}}}}'
  loop: "{{ keycloak_pvcs }}"
  loop_control:
    label: "{{ item.metadata.name }}"

- name: Wait for PVC resize to reflect
  shell: >
    oc get pvc {{ item.metadata.name }} -n {{ namespace }} -o json | jq -r '.status.capacity.storage'
  register: pvc_capacity
  until: pvc_capacity.stdout == new_size
  retries: 15
  delay: 10
  loop: "{{ keycloak_pvcs }}"
  loop_control:
    label: "{{ item.metadata.name }}"

- name: Gather Keycloak pods
  command: >
    oc get pods -n {{ namespace }} -l postgres-operator.crunchydata.com/cluster=keycloak -o json
  register: keycloak_pods_raw

- name: Set fact with parsed pods
  set_fact:
    keycloak_pods: "{{ (keycloak_pods_raw.stdout | from_json)['items'] }}"

- name: Delete Keycloak pods to pick up new PVC size
  command: >
    oc delete pod {{ item.metadata.name }} -n {{ namespace }}
  loop: "{{ keycloak_pods }}"
  loop_control:
    label: "{{ item.metadata.name }}"

- name: Wait for pods to be running again
  shell: >
    oc get pod {{ item.metadata.name }} -n {{ namespace }} -o json | jq -r '.status.phase'
  register: pod_status
  until: pod_status.stdout == "Running"
  retries: 20
  delay: 15
  loop: "{{ keycloak_pods }}"
  loop_control:
    label: "{{ item.metadata.name }}"

- name: Show final PVC and pod status
  block:
    - name: PVC status
      command: oc get pvc -n {{ namespace }}

    - name: Pods status
      command: oc get pods -n {{ namespace }}

- name: Patch the OpenShift Console resource to set logoutRedirect
  command: >
    oc patch console cluster
    --type=merge
    -p '{"spec": {"authentication": {"logoutRedirect": "{{ logout_redirect_url }}"}}}'
  register: patch_result

- name: Show patch result
  debug:
    var: patch_result.stdout

- name: Apply KeycloakRealmImport custom resource
  copy:
    dest: /tmp/keycloak_realm_import.yaml
    content: |
      apiVersion: k8s.keycloak.org/v2alpha1
      kind: KeycloakRealmImport
      metadata:
        name: trusted-artifact-signer
        namespace: tssc-keycloak
      spec:
        keycloakCRName: keycloak
        realm:
          id: trusted-artifact-signer
          realm: trusted-artifact-signer
          displayName: "Trusted Artifact Signer"
          enabled: true
          clients:
            - clientId: ocp4_workload_advanced_developer_suite_openshift_keycloak_client_id
              enabled: true
              protocol: openid-connect
              redirectUris:
                - "{{ ocp_console_url }}/*"
              attributes:
                post.logout.redirect.uris: "{{ ocp_console_url }}"

- name: Apply KeycloakRealmImport using oc
  command: >
    oc apply -f /tmp/keycloak_realm_import.yaml

- name: Debug kubeconfig path
  debug:
    msg: "{{ kubeconfig_path }}"

- name: Retrieve gitops credentials
  kubernetes.core.k8s_info:
    api_version: v1
    kind: Secret
    name: tssc-gitops-cluster
    namespace: "tssc-gitops"
  register: r_gitops_credentials
  retries: 120
  delay: 10
  until:
    - r_gitops_credentials is defined
    - r_gitops_credentials.resources is defined
    - r_gitops_credentials.resources | length > 0

- name: Set GitOps admin password fact
  set_fact:
    argo_password: "{{ r_gitops_credentials.resources[0].data['admin.password'] | b64decode }}"

- name: Debug GitOps password (optional, remove in production)
  debug:
    msg: "ArgoCD admin password is {{ argo_password }}"

- name: Get Argo CD route
  shell: >
    oc -n {{ argo_namespace }} get route tssc-gitops-server -o jsonpath='{.spec.host}'
  register: argo_route
  changed_when: false

- name: Set Argo CD host fact
  set_fact:
    argo_host: "{{ argo_route.stdout }}"

#- name: Log in to Argo CD CLI
#  shell: >
#    /usr/local/bin/argocd login {{ argo_host }}
#    --username {{ argo_username }}
#    --password {{ argo_password }}
#    --grpc-web
#    --insecure
#  register: argo_login
#  changed_when: false

#- name: Get current Kubernetes context
#  shell: kubectl config current-context
#  register: current_context
#  changed_when: false

#- name: Remove existing in-cluster registration (if exists)
#  shell: >
#    /usr/local/bin/argocd cluster rm https://kubernetes.default.svc --grpc-web --yes || true
#  register: remove_cluster
#  changed_when: false

#- name: Add OpenShift cluster to Argo CD with all namespaces
#  shell: >
#    /usr/local/bin/argocd cluster add {{ current_context.stdout }}
#    --name in-cluster
#    --grpc-web
#    --yes
#    --upsert
#  environment:
#    KUBECONFIG: "{{ kubeconfig_path }}"
#  register: add_cluster
#  changed_when: "'Cluster' in add_cluster.stdout or 'created' in add_cluster.stdout"

#- name: List registered clusters
#  shell: /usr/local/bin/argocd cluster list --grpc-web
#  register: cluster_list
#  changed_when: false

#- name: Show cluster registration result
#  debug:
#    msg: "{{ cluster_list.stdout_lines }}"

- name: Create dynamic-plugins-npmrc Secret in OpenShift
  kubernetes.core.k8s:
    state: present
    definition:
      apiVersion: v1
      kind: Secret
      metadata:
        name: dynamic-plugins-npmrc
        namespace: tssc-dh
      type: Opaque
      data:
        .npmrc: "{{ '@redhat:registry=https://npm.registry.redhat.com' | b64encode }}"

